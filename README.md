# involta

## Run App

```bash
# install dependencies
$ npm install

# install pm2

$ npm install -g pm2

# start redis in Docker

$ docker-compose run

# start the app
# dev start 
$ npm run dev
# deploy
$ npm run bild
$ npm run start

# run pm2 daemon
$ pm2 start ./daemon.config.js
```
